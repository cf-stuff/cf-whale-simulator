import Stats from "./stats.js";

const StarAltar = {
  str: {
    stat: Stats.str.name,
    max: 240
  },
  dex: {
    stat: Stats.dex.name,
    max: 240
  },
  sta: {
    stat: Stats.sta.name,
    max: 240
  },
  atkPercent: {
    stat: Stats.atkPercent.name,
    max: 20
  },
  def: {
    stat: Stats.def.name,
    max: 200
  },
  eva: {
    stat: Stats.eva.name,
    max: 200
  },
  hpPercent: {
    stat: Stats.hpPercent.name,
    max: 20
  },
  hp: {
    stat: Stats.hp.name,
    max: 200
  },
  crt: {
    stat: Stats.crt.name,
    max: 200
  },
  spd: {
    stat: Stats.spd.name,
    max: 20
  },
  brk: {
    stat: Stats.brk.name,
    max: 200
  },
  hit: {
    stat: Stats.hit.name,
    max: 200
  },
  res: {
    stat: Stats.res.name,
    max: 200
  },
  minAtk: {
    stat: Stats.minAtk.name,
    max: 50
  },
  maxAtk: {
    stat: Stats.maxAtk.name,
    max: 50
  },
}

export default StarAltar;
